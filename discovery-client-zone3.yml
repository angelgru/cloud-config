server:
  port: ${PORT:8083}
eureka:
  instances:
    metadataMap:
      zone: zone3
  client:
    service-url:
      default-zone: http://localhost:8763/eureka/, http://localhost:8761/eureka/, http://localhost:8762/eureka/
endpoints:
  shutdown:
    enabled: true
zone: zone3