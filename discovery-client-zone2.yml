server:
  port: ${PORT:8082}
eureka:
  instances:
    metadataMap:
      zone: zone2
  client:
    service-url:
      default-zone: http://localhost:8762/eureka/, http://localhost:8761/eureka/, http://localhost:8763/eureka/
endpoints:
  shutdown:
    enabled: true

zone: zone2